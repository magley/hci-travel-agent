<Window x:Class="YouTravel.Shared.ConfirmBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:YouTravel.Shared"
        mc:Ignorable="d"
		Style="{StaticResource WinDark}"
		WindowStartupLocation="CenterOwner"
		ResizeMode="NoResize"
        Height="160" Width="400">
    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>
		<TextBlock Grid.Row="0" TextWrapping="Wrap" Text="{Binding MessageBody}" VerticalAlignment="Center" Margin="16 0 16 0"/>
		<StackPanel Grid.Row="1" Orientation="Horizontal" Margin="10">
			<Button x:Name="btnYes" Click="btnYes_Click" Width="100" Margin="16 0 0 0">Yes</Button>
			<Button x:Name="btnNo" Click="btnNo_Click" Width="100" Margin="16 0 0 0">No</Button>
		</StackPanel>
	</Grid>
</Window>
