<Window x:Class="YouTravel.Agent.ArrangementEdit"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:YouTravel.Agent"
		xmlns:sysglb="clr-namespace:System.Globalization;assembly=mscorlib"
		xmlns:sys="clr-namespace:System;assembly=mscorlib"
		mc:Ignorable="d"
		Style="{StaticResource WinDark}"
        Title="ArrangementEdit" Height="640" Width="640">

	<Window.Resources>
		<BitmapImage x:Key="IcoSave" UriSource="../Res/IcoPageEdit.png"/>
		<BitmapImage x:Key="IcoPublish" UriSource="../Res/IcoDisk.png"/>
	</Window.Resources>

	<Grid Margin="16">
		<Grid.RowDefinitions>
			<RowDefinition Height="9*"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<GroupBox Grid.Row="0" Margin="5">
			<TabControl>
				<TabItem Header="General">
					<StackPanel Margin="5">
						<StackPanel Margin="0 10 0 0" Orientation="Horizontal">
							<Label Width="150" Content="Name"/>
							<TextBox Width="200" Text="{Binding Name, Mode=TwoWay}"/>
						</StackPanel>
						<DockPanel LastChildFill="True" Margin="0 10 0 0">
							<Label Width="150" Content="Description"/>
							<TextBox Text="{Binding Description, Mode=TwoWay}"/>
						</DockPanel>
						<StackPanel Margin="0 10 0 0" Orientation="Horizontal">
							<Label Width="150" Content="Price (€)"/>
							<TextBox TextAlignment="Right" Width="100"
									 Text="{Binding Price, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay, StringFormat='#.00', ConverterCulture={x:Static sysglb:CultureInfo.CurrentCulture}}"/>
						</StackPanel>
						<StackPanel Margin="0 10 0 0" Orientation="Horizontal">
							<Label Width="150" Content="When"/>
							<Calendar x:Name="arrangementCalendar" SelectionMode="SingleRange" SelectedDatesChanged="Calendar_SelectedDatesChanged" IsTodayHighlighted="True" DisplayDateStart="{x:Static sys:DateTime.Today}"/>
						</StackPanel>
					</StackPanel>
				</TabItem>
				<TabItem Header="Activities">

				</TabItem>
			</TabControl>
		</GroupBox>

		<Grid Grid.Row="1">
			<StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
				<Button Margin="0 0 10 0">
					<DockPanel HorizontalAlignment="Stretch">
						<Image Source="{StaticResource IcoSave}" Height="20" Margin="5 0 5 0"/>
						<Label Content="Save Draft"/>
					</DockPanel>
				</Button>

				<Button>
					<DockPanel HorizontalAlignment="Stretch">
						<Image Source="{StaticResource IcoPublish}" Height="20" Margin="5 0 5 0"/>
						<Label Content="Publish Changes"/>
					</DockPanel>
				</Button>
			</StackPanel>
		</Grid>
	</Grid>

</Window>
